# GI & Hepatology Weekly digest — GitHub Actions runner deps.
# gemini.py uses gemini-webapi (cookie auth) + curl_cffi; stdlib elsewhere.
# matplotlib renders the evidence-mix chart locally (no API needed).
gemini-webapi>=2.0.0
curl_cffi>=0.7.0
loguru
browser-cookie3
matplotlib>=3.8
notebooklm-py>=0.8.1
